const Sinistralidade = () => {
  return (
    <section className="bg-card rounded-xl border border-border shadow-sm p-6">
      <div className="flex flex-col items-center justify-center text-center py-16 gap-2">
        <h2 className="text-lg font-semibold text-foreground">Tabela Sinistralidade</h2>
        <p className="text-sm text-muted-foreground max-w-md">
          Nenhuma fonte de dados conectada. Aguardando a nova tabela de sinistralidade para ser
          vinculada a este submenu.
        </p>
      </div>
    </section>
  );
};

export default Sinistralidade;
